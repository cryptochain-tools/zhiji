# /etc/zhiji/migrate.env — root-owned, group-readable only by zhiji-migrate.
# This account may alter the zhiji schema only; it must not be used by web/worker.
DATABASE_URL=postgresql://<zhiji_migration_user>:<url-encoded-password>@127.0.0.1:5432/zhiji
